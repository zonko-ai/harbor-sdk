{
  "name": "harbor-sdk",
  "private": true,
  "type": "module",
  "packageManager": "bun@1.3.11",
  "workspaces": [
    "packages/*",
    "examples/*"
  ],
  "scripts": {
    "typecheck": "bun run --filter browser-client-example typecheck && bun run --filter client-promise-example typecheck && bun run --filter sdk-system-example typecheck && bun run --filter local-platform-example typecheck",
    "smoke": "bun run typecheck && bun run --filter client-promise-example start && bun run --filter sdk-system-example start && bun run --filter local-platform-example start",
    "pack:dry-run": "npm pack --dry-run ./packages/client && npm pack --dry-run ./packages/sdk"
  },
  "catalog": {
    "effect": "4.0.0-beta.66",
    "typescript": "^5.9.3"
  },
  "devDependencies": {
    "@types/node": "^22.0.0",
    "typescript": "catalog:"
  },
  "trustedDependencies": [
    "esbuild"
  ]
}
